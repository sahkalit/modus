Template.chatTalk.events({
	'button.send-message': function(e) {
		console.log(data);
		// var chat = Chat.findOne({_id: Router.current().params._id});
		// var message = {
		// 	message: "message",
		// 	chatId: chat._id,
		// 	creatorId: Meteor.userId()
		// 	// title: $(e.target).find('[name=title]').val()
		// };

		// Messages.insert(message);
	}
});